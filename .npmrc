# Ensure scoped packages are published publicly by default
access=public

# Save precise versions in package.json (no carets/tildes) for stability
save-exact=true

# Use the standard registry
registry=https://registry.npmjs.org/